# Node
NODE_ENV=development
PORT=3000

# 接口全局前缀（例如 /dev-api，留空则不加前缀）
API_BASE_PATH=/dev-api

# 时区
TZ=Asia/Shanghai

# MySQL（应用在宿主机运行，连接到本机映射端口）
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=adui
DB_PASS=adui_pass
DB_NAME=adui_rdms
DB_SYNC=false
TYPEORM_AUTOLOAD=false
# 仅用于容器内 MySQL root 账户（本机应用通常不用 root）
DB_ROOT_PASS=root_pass

# Redis（应用在宿主机运行，连接到本机映射端口）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# 可选：如需为 Redis 设置密码，请取消注释并与 docker-compose 中 REDIS_PASSWORD 保持一致
# REDIS_PASSWORD=your_redis_pass

# JWT / 其他应用密钥（请替换为你自己的值）
JWT_SECRET=your_jwt_secret

# Swagger
SWAGGER_ENABLED=true
SWAGGER_PATH=/docs
OPENAPI_JSON_PATH=/openapi-json

# Logger
LOG_LEVEL=debug
LOG_DIR=logs

# 说明：
# - 如果你把 docker-compose 的端口映射改成了非 3306/6379（例如 13306/16379），
#   记得把 DB_PORT/REDIS_PORT 同步改为对应端口。
# - 若未来将应用容器化并与 db/redis 处于同一 Docker 网络，可将 DB_HOST/REDIS_HOST 改回服务名：db、redis。